<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:label="Battery Gradient" android:theme="@android:style/Theme.Material.Light.NoActionBar" android:hasCode="true">
        <activity android:name=".SettingsActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider android:name=".SettingsProvider" android:authorities="dev.chet.batterygradient.settings" android:exported="true" />
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="Battery only: filled, dashed, and circular gradient icons" />
        <meta-data android:name="xposedminversion" android:value="82" />
    </application>
</manifest>
